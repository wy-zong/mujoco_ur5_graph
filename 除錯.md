# BiSOFollower `--robot.cameras` 問題分析與修改思路

## 問題描述

在使用 `lerobot-record` 搭配 `--robot.type=bi_so_follower` 時，以下兩種相機傳入方式各有缺陷：

| 傳入方式 | 結果 |
|---|---|
| `--robot.cameras='{...}'` | 報錯：`cameras` 不是 `BiSOFollowerConfig` 的有效欄位 |
| `--robot.left_arm_config.cameras='{...}'` | 可正常蒐集資料，但觀測 key 被加上 `left_` 前綴，變成 `observation.images.left_camera1` |

理想行為是：相機放在頂層 (`--robot.cameras`)，觀測 key 維持 `camera1`、`camera2`、`camera3`（無前綴），與訓練資料一致。

---

## 根本原因分析

### 這是 PR #2780（2026-01-12）引入的 regression

PR **#2780**（commit `d791a431`，作者 Steven Palma，標題 "feat(robots): consolidates bi SO setups"）在重構時，**遺漏了將 `cameras` 欄位從舊架構搬移到新架構**。

#### 舊版（`BiSO100FollowerConfig`，已被 #2780 刪除）

```python
@RobotConfig.register_subclass("bi_so100_follower")
@dataclass
class BiSO100FollowerConfig(RobotConfig):
    left_arm_port: str
    right_arm_port: str
    ...
    # cameras (shared between both arms)  ← 明確標注：相機是兩臂共用
    cameras: dict[str, CameraConfig] = field(default_factory=dict)
```

```python
# bi_so100_follower.py
left_arm_config = SO100FollowerConfig(..., cameras={})   # ← 子臂傳空相機
right_arm_config = SO100FollowerConfig(..., cameras={})  # ← 子臂傳空相機
self.cameras = make_cameras_from_configs(config.cameras) # ← 頂層直接管理
```

`_cameras_ft` 不加任何前綴，觀測 key 就是 `camera1`、`camera2`。

#### 新版（`BiSOFollowerConfig`，#2780 引入）

```python
@RobotConfig.register_subclass("bi_so_follower")
@dataclass
class BiSOFollowerConfig(RobotConfig):
    left_arm_config: SOFollowerConfig   # ← cameras 被藏進子臂 config
    right_arm_config: SOFollowerConfig  # ← 頂層沒有 cameras 欄位（遺漏）
```

```python
# bi_so_follower.py
cameras=config.left_arm_config.cameras  # ← 相機被委託給子臂管理
```

`_cameras_ft` 全部加 `left_`/`right_` 前綴，觀測 key 變成 `left_camera1`。

#### 為何 `left_`/`right_` 前綴邏輯看起來更早就存在

`left_`/`right_` 前綴的邏輯在 commit `0938a1d8`（8個月前）就存在，但當時相機是**頂層管理**（不經過子臂），前綴邏輯只影響馬達，不影響相機。
真正讓相機受到前綴影響，是 **#2780 把相機移進子臂時造成的**，等於是重構時遺漏了「頂層 cameras 的搬移」。

---

### 1. `BiSOFollowerConfig` 缺少 `cameras` 欄位

**檔案**：`lerobot/src/lerobot/robots/bi_so_follower/config_bi_so_follower.py`

```python
@RobotConfig.register_subclass("bi_so_follower")
@dataclass
class BiSOFollowerConfig(RobotConfig):
    left_arm_config: SOFollowerConfig
    right_arm_config: SOFollowerConfig
    # 沒有 cameras 欄位（#2780 重構時遺漏）
```

draccus 的 CLI 參數用點號（`.`）表示巢狀 dataclass 的欄位路徑，逐層查找：

- `--robot.cameras` → 在 `BiSOFollowerConfig` 直接找 `cameras`，**找不到** → `DecodingError`
- `--robot.left_arm_config.cameras` → `BiSOFollowerConfig.left_arm_config`（型別 `SOFollowerConfig`）→ `SOFollowerConfig.cameras`（存在）→ 成功解析

`--robot.left_arm_config.cameras` 能用，純粹是因為 `cameras` 欄位存在於路徑末端的 `SOFollowerConfig`，與雙臂設計無關。

### 2. `BiSOFollower.get_observation()` 對所有子臂觀測加上 `left_`/`right_` 前綴

**檔案**：`lerobot/src/lerobot/robots/bi_so_follower/bi_so_follower.py`，第 122–133 行

```python
def get_observation(self) -> RobotObservation:
    obs_dict = {}
    left_obs = self.left_arm.get_observation()
    obs_dict.update({f"left_{key}": value for key, value in left_obs.items()})  # 全部加 left_
    right_obs = self.right_arm.get_observation()
    obs_dict.update({f"right_{key}": value for key, value in right_obs.items()})  # 全部加 right_
    return obs_dict
```

因為 `get_observation()` 對左臂所有回傳值（含相機）一律加 `left_` 前綴，所以：
- 馬達：`shoulder_pan.pos` → `left_shoulder_pan.pos`（正確，需要區分左右）
- 相機：`camera1` → `left_camera1`（不正確，相機是共用的，不屬於特定手臂）

同樣的前綴邏輯也出現在 `_cameras_ft`（第 83–85 行）。

### 3. `BiSOFollower.__init__` 的相機合併不影響觀測 key

第 66 行：
```python
self.cameras = {**self.left_arm.cameras, **self.right_arm.cameras}
```

這行只是為了讓 `robot.cameras` 屬性存在（相容性），但實際觀測資料的 key 名稱由 `get_observation()` 決定，因此這行的 key 沒有前綴，但真正錄製到資料集的 key 仍然有前綴。

---

## 修改思路

需要修改兩個檔案，**共五個位置**：

### 檔案一：`config_bi_so_follower.py`

**修改**：加入頂層 `cameras` 欄位

```python
from dataclasses import dataclass, field
from lerobot.cameras import CameraConfig
from lerobot.robots.so_follower import SOFollowerConfig
from ..config import RobotConfig

@RobotConfig.register_subclass("bi_so_follower")
@dataclass
class BiSOFollowerConfig(RobotConfig):
    left_arm_config: SOFollowerConfig
    right_arm_config: SOFollowerConfig
    # 新增：頂層相機，觀測 key 不加 arm 前綴
    cameras: dict[str, CameraConfig] = field(default_factory=dict)
```

### 檔案二：`bi_so_follower.py`

需修改以下五個位置：

#### (1) 新增 import

```python
from lerobot.cameras.utils import make_cameras_from_configs
```

#### (2) `__init__`：初始化頂層相機，並合併進 `self.cameras`

```python
# 初始化頂層相機（不屬於任何一隻手臂）
self.top_cameras = make_cameras_from_configs(config.cameras)
# 更新 self.cameras，同時包含頂層相機
self.cameras = {**self.left_arm.cameras, **self.right_arm.cameras, **self.top_cameras}
```

#### (3) `_cameras_ft`：加入頂層相機（不加前綴）

```python
@property
def _cameras_ft(self) -> dict[str, tuple]:
    top_cameras_ft = {
        cam: (self.config.cameras[cam].height, self.config.cameras[cam].width, 3)
        for cam in self.top_cameras
    }
    return {
        **{f"left_{k}": v for k, v in self.left_arm._cameras_ft.items()},
        **{f"right_{k}": v for k, v in self.right_arm._cameras_ft.items()},
        **top_cameras_ft,  # 不加前綴
    }
```

#### (4) `connect` / `is_connected` / `disconnect`：處理頂層相機的生命週期

```python
@property
def is_connected(self) -> bool:
    top_ok = all(cam.is_connected for cam in self.top_cameras.values())
    return self.left_arm.is_connected and self.right_arm.is_connected and top_ok

def connect(self, calibrate: bool = True) -> None:
    self.left_arm.connect(calibrate)
    self.right_arm.connect(calibrate)
    for cam in self.top_cameras.values():
        cam.connect()

def disconnect(self):
    self.left_arm.disconnect()
    self.right_arm.disconnect()
    for cam in self.top_cameras.values():
        cam.disconnect()
```

#### (5) `get_observation`：讀取頂層相機（不加前綴）

```python
def get_observation(self) -> RobotObservation:
    obs_dict = {}
    left_obs = self.left_arm.get_observation()
    obs_dict.update({f"left_{key}": value for key, value in left_obs.items()})
    right_obs = self.right_arm.get_observation()
    obs_dict.update({f"right_{key}": value for key, value in right_obs.items()})
    # 頂層相機直接寫入，不加前綴
    for cam_key, cam in self.top_cameras.items():
        obs_dict[cam_key] = cam.read_latest()
    return obs_dict
```

---

## 修改後的行為

| 傳入方式 | 修改後結果 |
|---|---|
| `--robot.cameras='{...}'` | 正常解析，觀測 key 為 `camera1`、`camera2`、`camera3`（無前綴） |
| `--robot.left_arm_config.cameras='{...}'` | 仍可用，觀測 key 為 `left_camera1`（有前綴，適合相機確實屬於特定手臂的情境） |

---

## 注意事項

- `read_latest()` 是 `so_follower.py` 修改後使用的方法名稱（對應原本的 `async_read()`），需確認 camera 物件的實際方法名稱。
- 頂層相機的 `connect()` 應在兩臂連線後呼叫，避免干擾校準流程。
- 若政策訓練時使用的是 `camera1`（無前綴），則必須使用頂層 `--robot.cameras` 才能正確對應觀測 key，不能使用 `left_arm_config.cameras`。
